#pragma once

// Copy to secrets.h and fill in real values. Keep secrets.h out of version
// control (see .gitignore).

static const char* WIFI_SSID     = "your-wifi-ssid";
static const char* WIFI_PASSWORD = "your-wifi-password";

static const char* MQTT_HOST     = "192.168.1.10";
static const uint16_t MQTT_PORT  = 1883;
static const char* MQTT_USER     = "mqttuser";
static const char* MQTT_PASSWORD = "mqttpass";

// Deliberately different from MQTT_PASSWORD — OTA is a remote-flash surface,
// worth keeping on its own credential rather than reusing the broker password.
static const char* OTA_PASSWORD  = "choose-an-ota-password";
